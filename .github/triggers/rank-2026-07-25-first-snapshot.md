# Rank Tracker trigger — first real snapshot

Runs `track run` for all 7 tracked keywords (Snap Apps AI + the 6-review
LinkedIn backlog) to record the first live Serper positions, instead of waiting
for Monday's cron.
