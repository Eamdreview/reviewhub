# Weekly Affiliate Intelligence — on-demand run

Committing/updating a file under `.github/triggers/weekly-*` triggers one run of
the Weekly Affiliate Intelligence pipeline (see `weekly-intelligence.yml`). The
pipeline generates `reports/<YYYY-MM-DD>.md` and commits it back to `main`.

Requested: 2026-07-14 (real run, latest code on main).
