# Secrets
.env

# Intermediate stage dumps — never committed. The SQLite databases under
# data/history/ and data/knowledge/ ARE committed (they must survive across
# weekly runs); only the stage JSON is ignored.
data/*.json
*.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
